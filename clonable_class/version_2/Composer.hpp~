#include "Derived.hpp"
#include "../../../pacs-examples/Examples/src/Utilities/CloningUtilities.hpp"

class Composer
{
public:
  Composer() = default;
  Composer(ptr_type c);
  Composer(Composer const & c);  
  Composer & operator=(Composer const& c);
private:
  apsc::PointerWrapper<Base> component;
};
